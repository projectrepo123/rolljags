// Weeks planned for the season, so a week can show as "Coming soon" on the
// site before any photos exist for it. Add a weekNum here ahead of a game;
// once photos are uploaded for that week, real data takes over automatically
// and this entry becomes redundant (harmless to leave, or remove it).
//
// To start a new season, add a new year key with its own list of weeks.
export const SCHEDULE = {
  "2026": ["01"],
};
